components:
  schemas:
    ChildDTO:
      type: object
      description: Data element for Child
      required:
        - email
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
        firstName:
          type: string
          minLength: 3
        lastName:
          type: string